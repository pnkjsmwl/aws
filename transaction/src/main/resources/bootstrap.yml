spring:
  cloud:
    config: 
      uri: ${CLOUD_CONFIG_URI:http://localhost:8080}
  application:
    name: TransactionMS 
transaction_crud:
  url: ${TRANSACTION_CRUD_URL:http://localhost:8096}